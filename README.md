From the top bar, click on terminal -> new terminal

Only do this first time: run npm install
In the terminal, run npm run start

Any time you make changes to a file, just save the file and your localhost should auto update

When you want to push changes to main website, run in a new terminal

git add .
git commit -m "DESCRIPTION OF CHANGES"
git push